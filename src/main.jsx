import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import { motion, useMotionValue, useSpring } from "motion/react";
import { ArrowLeftRight, Check, Copy, Download, FileText, Heart, Link2, Mic, Volume2, Image as ImageIcon, Languages, RefreshCw, Share2, Sparkles, Trash2, ClipboardPaste, WandSparkles, Upload } from "lucide-react";
import React from "react";
import "./styles.css";

const EASE = [0.16, 1, 0.3, 1];

function Footer() {
  return <motion.footer className="footer" initial={{ y: 20, opacity: 0 }} animate={{ y: 0, opacity: 1 }} transition={{ delay: 0.5, duration: 1, ease: EASE }}><div className="footer-left"><motion.div className="subtitle" initial={{ y: 16, opacity: 0 }} animate={{ y: 0, opacity: 1 }} transition={{ delay: 0.6, duration: 0.8, ease: EASE }}><i />AI-powered language intelligence</motion.div><motion.h1 initial={{ y: 20, opacity: 0 }} animate={{ y: 0, opacity: 1 }} transition={{ delay: 0.8, duration: 0.8, ease: EASE }}>Translate anything.<br />Understand everyone.</motion.h1><motion.div className="actions" initial={{ y: 16, opacity: 0 }} animate={{ y: 0, opacity: 1 }} transition={{ delay: 1, duration: 0.8, ease: EASE }}><a className="button primary" href="/translator">Start translating</a><a className="button secondary" href="/features">Explore features</a></motion.div></div><a className="scroll-cue" href="/#universe"><span>Explore the model</span><b>↓</b></a></motion.footer>;
}

function TopNav() {
  return <motion.header className="top-nav" initial={{ y: -16, opacity: 0 }} animate={{ y: 0, opacity: 1 }} transition={{ duration: 0.8, ease: EASE }}><a className="nav-logo" href="/" aria-label="Home"><span className="logo-t">T</span></a><nav><a href="/translator">Translator</a><a href="/features">Features</a><a href="/#universe">About</a><a href="/login">Login</a></nav></motion.header>;
}

function Details() {
  return <section className="details"><motion.section id="features" className="detail-section" initial={{ opacity: 0, y: 35 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: .2 }} transition={{ duration: .8, ease: EASE }}><div className="detail-label">01 / CAPABILITIES</div><div className="detail-copy"><h2>Translation that<br /><em>moves with you.</em></h2><p>From a quick phrase to a live conversation, NeuralKinetics turns language into a fluid, human moment.</p></div><div className="capability-grid"><article><span>01</span><h3>Text intelligence</h3><p>Detect language automatically and translate across 150+ languages.</p></article><article><span>02</span><h3>Voice interface</h3><p>Speak naturally with voice-to-text, voice translation, and text-to-speech.</p></article><article><span>03</span><h3>Visual context</h3><p>Upload images and screenshots for fast OCR-powered translation.</p></article></div></motion.section><motion.section className="detail-section model-section" initial={{ opacity: 0, y: 35 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: .2 }} transition={{ duration: .8, ease: EASE }}><div className="detail-label">02 / MODEL LAYER</div><div className="model-grid"><h2>Built for<br /><em>meaning.</em></h2><div className="model-list"><div><b>Context aware</b><span>Preserves tone, intent, and conversational nuance.</span></div><div><b>Multimodal by design</b><span>Understands typed language, speech, and visual text together.</span></div><div><b>Adaptive output</b><span>Returns clear translations tuned to the way people communicate.</span></div></div></div></motion.section><motion.section id="how-it-works" className="detail-section workflow" initial={{ opacity: 0, y: 35 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: .2 }} transition={{ duration: .8, ease: EASE }}><div className="detail-label">03 / WORKFLOW</div><div className="workflow-line"><div><b>01</b><span>Speak or type</span></div><i /><div><b>02</b><span>Choose a language</span></div><i /><div><b>03</b><span>Understand instantly</span></div></div></motion.section><motion.section id="support" className="detail-section support-section" initial={{ opacity: 0, y: 35 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: .2 }} transition={{ duration: .8, ease: EASE }}><div className="detail-label">04 / READY WHEN YOU ARE</div><h2>Language has<br /><em>no limits.</em></h2><a href="/translator" className="outline-cta">Open translator <span>↗</span></a></motion.section></section>;
}

function AuthPage() {
  const [mode, setMode] = React.useState("signup");
  const [email, setEmail] = React.useState("");
  const submit = (event) => { event.preventDefault(); window.localStorage.setItem("translixor-auth", JSON.stringify({ email })); window.location.href = new URLSearchParams(window.location.search).get("redirect") || "/"; };
  return <main className="auth-page"><div className="auth-shell"><div className="auth-visual"><a className="auth-logo" href="/">T</a><a className="auth-back" href="/">Back to website <span>→</span></a><div className="auth-visual-copy"><small>TRANSLIXOR · LANGUAGE, UNDERSTOOD</small><h1>Turn every<br /><em>conversation</em><br />into connection.</h1><div className="auth-dots"><i /><i /><i className="active" /></div></div></div><div className="auth-form-wrap"><div className="auth-form"><p className="auth-kicker">TEXT · VOICE · VISION</p><h2>{mode === "signup" ? "Create an account" : "Welcome back"}</h2><p className="auth-switch">{mode === "signup" ? "Already have an account?" : "Need an account?"} <button type="button" onClick={() => setMode(mode === "signup" ? "login" : "signup")}>{mode === "signup" ? "Log in" : "Create one"}</button></p><form onSubmit={submit}>{mode === "signup" && <input placeholder="Your name" required />}<input type="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder="Email" required /><input type="password" placeholder="Enter your password" required /><label className="terms"><input type="checkbox" required /> <span>I agree to the <u>Terms & Conditions</u></span></label><button className="auth-submit" type="submit">{mode === "signup" ? "Create account" : "Log in"} <span>→</span></button></form><div className="auth-divider"><i />or continue with<i /></div><div className="social-auth"><button type="button" onClick={submit}>Google</button><button type="button" onClick={submit}>Apple</button></div></div></div></div></main>;
}

function TranslatorPage() {
  const [text, setText] = React.useState("");
  const [result, setResult] = React.useState("");
  const [from, setFrom] = React.useState("Auto detect");
  const [to, setTo] = React.useState("English");
  const [listening, setListening] = React.useState(false);
  const [isTranslating, setIsTranslating] = React.useState(false);
  const [copied, setCopied] = React.useState(false);
  const [saved, setSaved] = React.useState(false);
  const [authenticated] = React.useState(() => Boolean(window.localStorage.getItem("translixor-auth")));
  const [swapPulse, setSwapPulse] = React.useState(false);
  const textareaRef = React.useRef(null);
  const languages = ["English", "Hindi", "Spanish", "French", "German", "Japanese", "Arabic", "Chinese", "Portuguese", "Korean"];
  const languageCodes = { English: "en", Hindi: "hi", Spanish: "es", French: "fr", German: "de", Japanese: "ja", Arabic: "ar", Chinese: "zh", Portuguese: "pt", Korean: "ko" };
  React.useEffect(() => { if (textareaRef.current) { textareaRef.current.style.height = "auto"; textareaRef.current.style.height = `${Math.min(textareaRef.current.scrollHeight, 240)}px`; } }, [text]);
  const translate = async () => { if (!text.trim() || isTranslating) return; setIsTranslating(true); setResult(""); try { const response = await fetch("/api/translate", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ text, sourceLanguage: from === "Auto detect" ? "auto" : languageCodes[from], targetLanguage: languageCodes[to], sourceType: "text" }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || "Translation failed."); setResult(data.translation || data.translatedText || "Translation unavailable."); } catch (error) { setResult(`Translation unavailable: ${error.message}`); } finally { setIsTranslating(false); } };
  const listen = () => { const Recognition = window.SpeechRecognition || window.webkitSpeechRecognition; if (!Recognition) return setResult("Voice input is not supported in this browser."); const recognition = new Recognition(); recognition.lang = "en-US"; recognition.onstart = () => setListening(true); recognition.onend = () => setListening(false); recognition.onresult = (event) => setText((value) => `${value} ${event.results[0][0].transcript}`.trim()); recognition.start(); };
  const swapLanguages = () => { setFrom(to); setTo(from === "Auto detect" ? "English" : from); setSwapPulse(true); window.setTimeout(() => setSwapPulse(false), 450); };
  const speak = () => result && window.speechSynthesis?.speak(new SpeechSynthesisUtterance(result));
  const copyResult = async () => { if (!result) return; await navigator.clipboard?.writeText(result); setCopied(true); window.setTimeout(() => setCopied(false), 1400); };
  const downloadResult = () => { if (!result) return; const link = document.createElement("a"); link.href = URL.createObjectURL(new Blob([result], { type: "text/plain" })); link.download = "translation.txt"; link.click(); URL.revokeObjectURL(link.href); };
  const pasteText = async () => { const value = await navigator.clipboard?.readText(); if (value) setText((current) => `${current} ${value}`.trim()); };
  const fileReady = (label) => (event) => { const file = event.target.files?.[0]; if (file) setText(`${label}: ${file.name}`); };
  const requireAuth = (event) => { if (!authenticated) { event.preventDefault(); window.location.href = "/login?redirect=/translator"; } };
  React.useEffect(() => {
    if (authenticated) return undefined;
    const protectFeatures = (event) => {
      if (event.target.closest(".workspace-toolbar label, .smart-tool")) {
        event.preventDefault();
        event.stopPropagation();
        window.location.href = "/login?redirect=/translator";
      }
    };
    document.addEventListener("click", protectFeatures, true);
    return () => document.removeEventListener("click", protectFeatures, true);
  }, [authenticated]);
  return <section className="translator-page"><div className="translator-heading"><div><p className="translator-kicker"><Sparkles size={14} /> AI TRANSLATOR <span className="live-dot" /> LIVE WORKSPACE</p><h1>Every language.<br /><em>One conversation.</em></h1></div><p className="translator-intro">Translate text, speech, and images<br />in one intelligent workspace.</p></div><div className="translator-card"><div className="translator-card-topline"><span><i /> Workspace ready</span><b>TEXT · VOICE · VISION</b></div><div className="language-bar"><span><Languages size={15} /> Language pair</span><div className="language-control"><select value={from} onChange={(event) => setFrom(event.target.value)}><option>Auto detect</option>{languages.map((language) => <option key={language}>{language}</option>)}</select><button type="button" className={`swap-button ${swapPulse ? "pulse" : ""}`} onClick={swapLanguages} aria-label="Swap languages"><ArrowLeftRight size={16} /></button><select value={to} onChange={(event) => setTo(event.target.value)}>{languages.map((language) => <option key={language}>{language}</option>)}</select></div></div><div className="translation-grid"><div className="translation-panel source-panel"><small>01 / SOURCE <span>{from}</span></small><textarea ref={textareaRef} aria-label="Text to translate" value={text} onChange={(event) => setText(event.target.value)} placeholder="Type, paste or speak something..." /><div className="workspace-panel-footer"><div className="panel-actions workspace-toolbar"><button type="button" onClick={listen} className={listening ? "active" : ""}><Mic size={14} /> {listening ? "Listening" : "Voice Input"}</button><label><ImageIcon size={14} /> Image OCR<input type="file" accept="image/*" hidden onChange={fileReady("Image ready for OCR")} /></label><label><Upload size={14} /> Upload PDF<input type="file" accept="application/pdf" hidden onChange={fileReady("Document ready")} /></label><button type="button" onClick={pasteText}><ClipboardPaste size={14} /> Paste</button><button type="button" onClick={() => setText("")}><Trash2 size={14} /> Clear</button></div><span className="character-count">{text.length}/5000</span></div></div><div className="translation-panel output-panel"><div className="output-header"><small>02 / OUTPUT</small><span className="output-language-badge"><Languages size={13} /> {to}</span></div><div className={`translation-result ${result ? "has-result" : ""}`}>{isTranslating ? <span className="loading-shimmer">Translating language patterns…</span> : result || "Your translation will appear here."}</div><div className="panel-actions workspace-toolbar output-toolbar"><button type="button" onClick={speak}><Volume2 size={14} /> Listen</button><button type="button" onClick={copyResult}><Copy size={14} /> {copied ? "Copied" : "Copy"}</button><button type="button" onClick={() => setSaved(!saved)} className={saved ? "active" : ""}><Heart size={14} /> {saved ? "Saved" : "Save"}</button><button type="button" onClick={downloadResult}><Download size={14} /> Download</button><button type="button" onClick={translate}><RefreshCw size={14} /> Re-translate</button><button type="button" onClick={() => navigator.share?.({ title: "Translation", text: result })}><Share2 size={14} /> Share</button></div></div></div><div className="translator-action-bar"><span className="action-status">{isTranslating ? "Mapping context and meaning..." : result ? "Translation ready" : "Ready when you are"}</span><button type="button" className={`translate-action ${result && !isTranslating ? "is-success" : ""}`} disabled={!text.trim() || isTranslating} onClick={translate}>{isTranslating ? <span className="workspace-spinner" /> : result ? <Check size={16} /> : <Sparkles size={16} />}{isTranslating ? "Translating" : result ? "Translated" : "Translate"} <span>↗</span></button></div></div><div className="smart-tools"><div className="smart-tools-heading"><span>AI tools</span><small>Built for more than words</small></div><div className="smart-tools-grid"><article className="smart-tool"><Mic size={18} /><div><b>Voice Translation</b><span>Speak naturally across languages.</span></div></article><article className="smart-tool"><ImageIcon size={18} /><div><b>OCR Translation</b><span>Turn visual text into meaning.</span></div></article><article className="smart-tool"><FileText size={18} /><div><b>Document Translation</b><span>Keep files moving with context.</span></div></article><article className="smart-tool"><WandSparkles size={18} /><div><b>AI Grammar Fix</b><span>Polish tone before you send.</span></div></article></div></div></section>;
}

const storyFormats = ["Text", "PDF", "Word", "PowerPoint", "Excel", "Images", "Camera", "Voice", "Audio", "Videos", "Scanned Documents", "Handwriting", "Websites", "Emails", "Books", "Subtitles", "Notes"];
const storyLanguages = ["Hello", "Bonjour", "Hola", "नमस्ते", "こんにちは", "안녕하세요", "مرحبا", "Привет", "Ciao", "Olá", "你好", "สวัสดี", "Salam", "Shalom", "Merhaba", "Jambo"];

function StorySections() {
  const [demoText, setDemoText] = React.useState("");
  return <section className="story"><motion.section id="universe" className="story-block universe" initial={{ opacity: 0 }} whileInView={{ opacity: 1 }} viewport={{ once: true }}><div className="word-cloud">{storyLanguages.map((word, index) => { const lane = Math.floor(index / 2); const left = index % 2 === 0 ? 4 + (lane % 3) * 7 : 76 - (lane % 3) * 5; return <span key={word} style={{ "--i": index, left: `${left}%`, top: `${8 + lane * 6}%` }}>{word}</span>; })}</div><div className="story-center"><small>01 / AI LANGUAGE UNIVERSE</small><h2>Every language deserves to be understood.</h2><p>Powered by advanced AI capable of understanding context, meaning, tone, and intent.</p></div></motion.section><motion.section className="story-block why" initial={{ opacity: 0, y: 35 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: .2 }}><small>02 / WHY WE BUILT THIS</small><h2>Translation should<br /><em>never be a barrier.</em></h2><p>Most translators replace words. Our AI understands what people mean, wherever language takes them.</p><span className="meaning-anchor" aria-label="Hover to translate Japanese greeting" /><div className="scenario-row"><span>Research</span><span>Travel</span><span>Business</span><span>Medical</span><span>Legal</span><span>Code</span></div></motion.section></section>;
}

function EndMark() {
  return <motion.section className="end-mark" initial={{ opacity: 0, y: 30 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: .25 }} transition={{ duration: .9, ease: EASE }}><div className="end-mark-meta"><span>TRANSLIXOR</span><span>Language, understood.</span></div><div className="end-mark-word">TRANSLIXOR</div></motion.section>;
}

function App() {
  const isTranslator = window.location.pathname === "/translator";
  const isLogin = window.location.pathname === "/login";
  const isFeatures = window.location.pathname === "/features";
  const cursorX = useSpring(useMotionValue(0), { stiffness: 90, damping: 18 });
  const cursorY = useSpring(useMotionValue(0), { stiffness: 90, damping: 18 });
  const cursorRotateY = useSpring(useMotionValue(0), { stiffness: 80, damping: 16 });
  const cursorRotateX = useSpring(useMotionValue(0), { stiffness: 80, damping: 16 });
  const handlePointerMove = (event) => { if (isTranslator || isFeatures) return; const x = (event.clientX / window.innerWidth - .5) * 2; const y = (event.clientY / window.innerHeight - .5) * 2; cursorX.set(x * 48); cursorY.set(y * 30); cursorRotateY.set(x * 7); cursorRotateX.set(y * -5); };
  const resetPointer = () => { cursorX.set(0); cursorY.set(0); cursorRotateX.set(0); cursorRotateY.set(0); };
  if (isLogin) return <AuthPage />;
  return <main id="top" onPointerMove={handlePointerMove} onPointerLeave={resetPointer} className={`page ${isTranslator || isFeatures ? "inner-route" : ""}`}>{!isTranslator && !isFeatures && <><motion.div className="video-wrap" style={{ x: cursorX, y: cursorY, rotateX: cursorRotateX, rotateY: cursorRotateY }} initial={{ opacity: 0, scale: 1.05 }} animate={{ opacity: 1, scale: 1 }} transition={{ duration: 0.65, ease: EASE }}><video autoPlay muted playsInline preload="auto" src="/robotic-hand.mp4" /></motion.div><Footer /><StorySections /><EndMark /></>}{isTranslator && <TranslatorPage />}{isFeatures && <Details />}<TopNav /></main>;
}

createRoot(document.getElementById("root")).render(<StrictMode><App /></StrictMode>);
